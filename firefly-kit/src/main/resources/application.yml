server:
  port: 8087
spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/detp}
    username: detp
    password: detp
firefly:
  base-url: ${FIREFLY_URL:http://localhost:8092}
  ws-url: ${FIREFLY_WS_URL:ws://localhost:8092/ws}
  listener-id: firefly-kit-1
saga:
  url: ${SAGA_URL:http://localhost:8086}
